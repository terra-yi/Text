# Text


## example

```echo Text::start()
    ->It
    ->is
    ->a
    ->wonderful
    ->text
    ->fullstop()
    ->fullstop(2)
    ->backspace()
    ->raw('more', 'text', '&', 'text', 'in', 'raw')
    ->comma()
    ->questionmark(3)
    ->newline()
    ->space()
    ->space()
    ->leftparenthesis()
    ->eol()
    ->fullstop(3)
    ->am
    ->I
    ->dreaming
    ->linebreak()
    ->or
    ->something
    ->fullstop(3)
    ->rightparenthesis()
    ,
    "\n";```
